apiVersion: apps/v1
kind: Deployment
metadata:
  name: smirvaki-llama-serving
  namespace: llm-training
spec:
  template:
    spec:
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      containers:
        - name: api
          image: docker.io/esmaeilmirvakili/llama-serving-gpu:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: LLAMA_GPU_LAYERS
              value: "-1"
              valueFrom: null
          resources:
            requests:
              nvidia.com/gpu: "1"
              cpu: "1"
              memory: 2Gi
            limits:
              nvidia.com/gpu: "1"
              cpu: "4"
              memory: 8Gi
